const TABS=[['overview','Overview'],['timeline','Timeline'],['documents','Documents'],['quotations','Quotations'],['travel','Travel'],['messages','Messages']];
const fmt=v=>v?new Date(v).toLocaleString('en-IN',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'}):'—';
export async function openCaseWorkspace({supabase,caseId,S,esc,money,write}){
  let tab='overview', data=null, busy=false;
  const root=document.createElement('div'); root.className='case-workspace';
  root.innerHTML='<div class="case-workspace-backdrop"></div><section class="case-workspace-panel"><div class="case-workspace-loading">Loading case workspace…</div></section>';
  document.body.appendChild(root);
  const close=()=>root.remove(); root.querySelector('.case-workspace-backdrop').onclick=close;
  const canWrite=!!S.staff&&!!write();
  const load=async()=>{
    const q=await supabase.from('ops_cases').select('*').eq('id',caseId).maybeSingle(); if(q.error)throw q.error;if(!q.data)throw Error('Case not found or access is not permitted.');
    const c=q.data, r=await Promise.all([
      supabase.from('ops_patients').select('full_name,phone,email,nationality,preferred_language').eq('id',c.patient_id).maybeSingle(),
      supabase.from('ops_centers').select('name').eq('id',c.center_id).maybeSingle(),
      supabase.from('ops_case_events').select('*').eq('case_id',caseId).order('created_at',{ascending:false}),
      supabase.from('ops_documents').select('*').eq('case_id',caseId).order('created_at',{ascending:false}),
      supabase.from('ops_quotations').select('*').eq('case_id',caseId).order('created_at',{ascending:false}),
      supabase.from('ops_itineraries').select('*').eq('case_id',caseId).maybeSingle(),
      supabase.from('ops_case_messages').select('*').eq('case_id',caseId).order('created_at'),
      supabase.from('ops_appointments').select('*').eq('case_id',caseId).order('appointment_date'),
      supabase.from('ops_tasks').select('*').eq('case_id',caseId).order('due_date'),
      supabase.from('ops_concierge').select('*').eq('case_id',caseId),
      supabase.from('ops_billing').select('*').eq('case_id',caseId)
    ]); r.forEach(x=>{if(x.error)throw x.error});
    data={c,p:r[0].data||{},center:r[1].data?.name||'',events:r[2].data||[],docs:r[3].data||[],quotes:r[4].data||[],itinerary:r[5].data||{},messages:r[6].data||[],appointments:r[7].data||[],tasks:r[8].data||[],concierge:r[9].data||[],billing:r[10].data||[]};
  };
  const action=async(fn)=>{if(busy)return;busy=true;render();try{await fn();await load()}catch(e){alert(e.message||'Operation failed')}finally{busy=false;render()}};
  const form=async(title,fields,fn)=>{
    const box=document.createElement('div');box.className='cw-dialog';
    box.innerHTML='<div class="cw-form"><h3>'+esc(title)+'</h3>'+fields.map(f=>'<label>'+esc(f.label)+(f.type==='textarea'?'<textarea data-f="'+f.key+'">'+esc(f.value||'')+'</textarea>':'<input data-f="'+f.key+'" type="'+(f.type||'text')+'" value="'+esc(f.value||'')+'">')+'</label>').join('')+'<div class="cw-form-actions"><button class="cw-secondary" data-x>Cancel</button><button class="cw-primary" data-s>Save</button></div></div>';
    root.appendChild(box);box.querySelector('[data-x]').onclick=()=>box.remove();box.querySelector('[data-s]').onclick=()=>{const v={};fields.forEach(f=>v[f.key]=box.querySelector('[data-f="'+f.key+'"]').value);box.remove();action(()=>fn(v))};
  };
  const add=(table,p)=>supabase.from(table).insert(p).then(r=>{if(r.error)throw r.error});
  const render=()=>{
    if(!data)return;
    const c=data.c,p=data.p;
    const body=tab==='overview'?'<div class="cw-grid cw-grid-3"><article class="cw-card"><h3>Patient</h3><b>'+esc(p.full_name||'')+'</b><p>'+esc(p.nationality||'')+' · '+esc(p.preferred_language||'')+'</p><p>'+esc(p.phone||'')+'<br>'+esc(p.email||'')+'</p></article><article class="cw-card"><h3>Care</h3><p><b>'+esc(c.specialty||'')+'</b>'+((c.procedure?' · '+esc(c.procedure):''))+'</p><p>Status <strong>'+esc(c.status)+'</strong><br>Estimated value <strong>'+money(c.estimated_value)+'</strong></p></article><article class="cw-card"><h3>Next</h3>'+data.appointments.slice(0,3).map(a=>'<p><b>'+esc(a.title)+'</b><br>'+esc(a.appointment_date)+' '+esc(a.appointment_time||'')+'</p>').join('')+'<p>'+data.tasks.filter(x=>x.status!=='Completed').length+' open task(s)</p></article><article class="cw-card cw-span-2"><h3>Journey</h3><div class="cw-journey">'+['ENQUIRY','ASSESSMENT','QUOTATION','ACCEPTED','TRAVEL PLANNED','IN TREATMENT','DISCHARGED','FOLLOW UP'].map(x=>'<span class="'+(x===c.status?'now':'')+'">'+x+'</span>').join('')+'</div></article><article class="cw-card"><h3>Financial</h3><p>'+data.billing.length+' invoice(s)</p><p>Outstanding <b>'+money(data.billing.filter(x=>x.status==='Due').reduce((s,x)=>s+Number(x.amount||0),0))+'</b></p></article></div>'
    :tab==='timeline'?'<div class="cw-section-head"><div><h2>Timeline</h2><p>Operational history.</p></div>'+(canWrite?'<button class="cw-primary" data-a="event">＋ Add event</button>':'')+'</div><div class="cw-timeline">'+(data.events.map(e=>'<article><span class="dot"></span><div><b>'+esc(e.event_type)+'</b><p>'+esc(e.message)+'</p><small>'+fmt(e.created_at)+'</small></div></article>').join('')||'<div class="cw-empty">No events yet.</div>')+'</div>'
    :tab==='documents'?'<div class="cw-section-head"><div><h2>Documents</h2><p>Document register, ready for Supabase Storage.</p></div>'+(canWrite?'<button class="cw-primary" data-a="doc">＋ Register document</button>':'')+'</div><div class="cw-list">'+(data.docs.map(d=>'<article><div><b>'+esc(d.file_name)+'</b><p>'+esc(d.category)+' · '+(d.visible_to_patient?'Patient visible':'Staff only')+'</p></div><small>'+fmt(d.created_at)+'</small></article>').join('')||'<div class="cw-empty">No documents.</div>')+'</div>'
    :tab==='quotations'?'<div class="cw-section-head"><div><h2>Quotations</h2><p>Hospital offers separate from billing.</p></div>'+(canWrite?'<button class="cw-primary" data-a="quote">＋ New quotation</button>':'')+'</div><div class="cw-list">'+(data.quotes.map(q=>'<article><div><b>'+esc(q.reference||'Quotation')+'</b><p>'+esc(q.status)+' · '+money(q.amount)+' · Valid '+esc(q.valid_until||'—')+'</p></div><small>'+fmt(q.created_at)+'</small></article>').join('')||'<div class="cw-empty">No quotations.</div>')+'</div>'
    :tab==='travel'?'<div class="cw-section-head"><div><h2>Travel & itinerary</h2><p>Flights, hotel, visa and attendants.</p></div>'+(canWrite?'<button class="cw-primary" data-a="travel">'+(data.itinerary.id?'Edit itinerary':'＋ Add itinerary')+'</button>':'')+'</div><div class="cw-grid cw-grid-2"><article class="cw-card"><h3>Flights</h3><p>'+esc(data.itinerary.departure_city||'—')+' → '+esc(data.itinerary.arrival_city||'—')+'</p><p>Departure: '+esc(data.itinerary.departure_date||'—')+'<br>Return: '+esc(data.itinerary.return_date||'—')+'</p><p>'+esc(data.itinerary.outbound_flight||'—')+' · '+esc(data.itinerary.return_flight||'—')+'</p></article><article class="cw-card"><h3>Stay & visa</h3><p>'+esc(data.itinerary.hotel_name||'—')+'</p><p>'+esc(data.itinerary.hotel_check_in||'—')+' → '+esc(data.itinerary.hotel_check_out||'—')+'</p><p>Visa: <b>'+esc(data.itinerary.visa_status||'PENDING')+'</b> · Attendants: '+Number(data.itinerary.attendants||0)+'</p></article><article class="cw-card cw-span-2"><h3>Notes</h3><p>'+esc(data.itinerary.notes||'No notes.')+'</p></article></div>'
    :'<div class="cw-section-head"><div><h2>Case messages</h2><p>Operational communication attached to this case.</p></div>'+(canWrite?'<button class="cw-primary" data-a="message">＋ Add message</button>':'')+'</div><div class="cw-messages">'+(data.messages.map(m=>'<article><b>Team</b><p>'+esc(m.body)+'</p><small>'+fmt(m.created_at)+'</small></article>').join('')||'<div class="cw-empty">No messages.</div>')+'</div>';
    root.querySelector('.case-workspace-panel').innerHTML='<header class="case-workspace-head"><div><div class="case-workspace-kicker">'+esc(c.case_code)+' · '+esc(c.status)+'</div><h1>'+esc(p.full_name||'Patient')+' · '+esc(c.specialty||'Case')+'</h1><p>'+esc(data.center)+' · '+esc(c.hospital||'Hospital not assigned')+' · '+esc(c.priority)+'</p></div><button class="case-workspace-close" data-close>×</button></header><nav class="case-workspace-tabs">'+TABS.map(t=>'<button class="'+(tab===t[0]?'active':'')+'" data-tab="'+t[0]+'">'+t[1]+'</button>').join('')+'</nav><main class="case-workspace-body">'+body+'</main>';
    root.querySelector('[data-close]').onclick=close;root.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});
    root.querySelector('[data-a="event"]')?.addEventListener('click',()=>form('Add timeline event',[{key:'event_type',label:'Event type',value:'NOTE'},{key:'message',label:'Message',type:'textarea'}],v=>add('ops_case_events',{case_id:caseId,event_type:v.event_type||'NOTE',message:v.message,actor_id:S.user.id})));
    root.querySelector('[data-a="doc"]')?.addEventListener('click',()=>form('Register document',[{key:'file_name',label:'File name'},{key:'category',label:'Category',value:'OTHER'},{key:'storage_path',label:'Storage path / URL'}],v=>add('ops_documents',{case_id:caseId,file_name:v.file_name,category:v.category||'OTHER',storage_path:v.storage_path||'',uploaded_by:S.user.id})));
    root.querySelector('[data-a="quote"]')?.addEventListener('click',()=>form('New quotation',[{key:'reference',label:'Reference'},{key:'amount',label:'Amount',type:'number'},{key:'valid_until',label:'Valid until',type:'date'},{key:'notes',label:'Notes',type:'textarea'}],v=>add('ops_quotations',{case_id:caseId,reference:v.reference,amount:Number(v.amount||0),currency:'AED',valid_until:v.valid_until||null,status:'DRAFT',notes:v.notes||'',created_by:S.user.id})));
    root.querySelector('[data-a="message"]')?.addEventListener('click',()=>form('Add case message',[{key:'body',label:'Message',type:'textarea'}],v=>add('ops_case_messages',{case_id:caseId,sender_id:S.user.id,body:v.body})));
    root.querySelector('[data-a="travel"]')?.addEventListener('click',()=>{const i=data.itinerary||{};form('Travel itinerary',[{key:'departure_city',label:'Departure city',value:i.departure_city},{key:'arrival_city',label:'Arrival city',value:i.arrival_city},{key:'departure_date',label:'Departure date',type:'date',value:i.departure_date},{key:'return_date',label:'Return date',type:'date',value:i.return_date},{key:'outbound_flight',label:'Outbound flight',value:i.outbound_flight},{key:'return_flight',label:'Return flight',value:i.return_flight},{key:'hotel_name',label:'Hotel',value:i.hotel_name},{key:'hotel_check_in',label:'Hotel check-in',type:'date',value:i.hotel_check_in},{key:'hotel_check_out',label:'Hotel check-out',type:'date',value:i.hotel_check_out},{key:'visa_status',label:'Visa status',value:i.visa_status||'PENDING'},{key:'attendants',label:'Attendants',type:'number',value:i.attendants||0},{key:'notes',label:'Notes',type:'textarea',value:i.notes}],v=>data.itinerary.id?supabase.from('ops_itineraries').update({...v,attendants:Number(v.attendants||0)}).eq('id',data.itinerary.id):add('ops_itineraries',{case_id:caseId,...v,attendants:Number(v.attendants||0)}))});
  };
  try{await load();render()}catch(e){root.querySelector('.case-workspace-panel').innerHTML='<div class="cw-error"><h2>Case workspace could not load</h2><p>'+esc(e.message)+'</p><button class="cw-primary" data-close>Close</button></div>';root.querySelector('[data-close]').onclick=close}
}
